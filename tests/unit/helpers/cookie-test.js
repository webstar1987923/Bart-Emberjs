
import { cookie } from 'bart/helpers/cookie';
import { module, test } from 'qunit';

module('Unit | Helper | cookie');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cookie([42]);
  assert.ok(result);
});

